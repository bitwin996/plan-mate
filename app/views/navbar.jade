nav.navbar.navbar-default.navbar-fixed-top(ng-controller="NavbarCtrl", role="navigation")

  .navbar-header
    a.navbar-brand(href="#") Plan Mate
    button.navbar-toggle(type="button", ng-click="toggle()")
      span.icon-bar
      span.icon-bar
      span.icon-bar

  //.navbar-header.navbar-right
    p.navbar-text
      a.navbar-link(href="#") Username

  .navbar-collapse(ng-class="{collapse:isCollapsed, in:!isCollapsed}")
    ul.nav.navbar-nav
      li(ng-class="{active:isActive('/mypage/plans')}")
        a(href="#/mypage/plans") Plan list

    ul.nav.navbar-nav.navbar-right
      li(ng-class="{active:isActive('/users')}", ng-show="isLoggedIn")
        //TODO: Get username
        a(href="#/mypage/plans") Test User

      // Login / Logout
      li(ng-class="{active:isActive('/auth')}", ng-show="!isLoggedIn")
        a(href="#/auth/login") Login
      li(ng-class="{active:isActive('/auth')}", ng-show="isLoggedIn")
        a(ng-click="logout()") Logout

